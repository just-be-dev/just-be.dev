---
import { formatDate } from '../utils/date';

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="date-component text-fg-2 inline-block">
	{formatDate(date)}
</time>
